# ask claude about the config file in the root and the build of the mocks in your test scripts
# obviously mocks only run in the test scripts so thats where i was building them but services can take in fakes
# so it might be necessary to not switch packages so much